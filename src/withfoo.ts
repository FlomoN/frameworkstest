/**
 * Multiplies a given number by two and Returns it
 * @param x A number to be multiplied
 * @returns double of the inputted number
 */
export default function foo(x: number): number {
  const b: number = x * 2;
  return (b);
}
